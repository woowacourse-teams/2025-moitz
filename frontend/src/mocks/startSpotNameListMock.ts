import { nameInfo } from '@shared/types/nameInfo';

const startSpotNameListMock: nameInfo[] = [
  { index: 0, name: '서울' },
  { index: 1, name: '강남' },
  { index: 2, name: '역삼' },
];

export default startSpotNameListMock;
